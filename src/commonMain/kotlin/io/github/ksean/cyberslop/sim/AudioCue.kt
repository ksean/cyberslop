package io.github.ksean.cyberslop.sim

/** A presentation-only sound transition emitted by one fixed simulation tick (PROD-102). */
enum class AudioCue {
    MeleeSwing,
    RangedFire,
    PsychicFire,
    PickupPulse,
}
